#include<stdio.h>
#include<string.h>
int main()
{
    int name[30];
    int count=0,i=0;
    printf("enter name:");
    gets(name);
    count=strlen(name); 
    printf("length of string is:%d",count);
}